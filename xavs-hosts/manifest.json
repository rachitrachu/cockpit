{
  "version": 1,
  "tools": {
    "xavs-hosts": {
      "label": "XAVS Hosts",
      "path": "index.html",
      "order": 20,
      "keywords": [
        {"matches": ["hosts", "cluster", "nodes", "inventory", "xavs"]},
        {"matches": ["deploy", "target", "configuration", "management", "ssh"]}
      ]
    }
  }
}
